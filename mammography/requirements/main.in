# DICOM deps
dicomsdl
# pydicom
# pylibjpeg
# pylibjpeg-libjpeg

# other stuff
# functorch<1.13.0  # compatible with torch 1.12.1
# functorch==0.1.1  # compatible with torch 1.11
# functorch
hydra-core
numexpr
omegaconf
opencv-python-headless
pandas
pytorch-lightning
torch
torchdata
torchmetrics
torchvision  #==0.12.0  # kaggle version
# torchvision==0.13.1; sys_platform != "linux"
# torchvision==0.13.1+cu116; sys_platform == "linux"  # compatible with torch 1.12.1
wandb  #<0.13.8 # something in 0.13.8 causes training to fail on kaggle

# --extra-index-url https://download.pytorch.org/whl/cu116
